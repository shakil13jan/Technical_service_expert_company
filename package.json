{
  "name": "technical_service_expert_company",
  "version": "1.0.0",
  "description": "`HTML`\r `CSS`\r `JavaScript`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.js'",
    "lint": "eslint '**/*.js'"
  }
}
